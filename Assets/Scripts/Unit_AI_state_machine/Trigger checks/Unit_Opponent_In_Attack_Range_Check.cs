using UnityEngine;

public class Unit_Opponent_In_Attack_Range_Check : MonoBehaviour
{
    //private void OnTriggerEnter(Collider other)
    //{
    //    var other_unit_component = other.GetComponent<Unit>();
    //    var parent_unit_component = GetComponentInParent<Unit>();
        
    //    update_unit_target(parent_unit_component, other_unit_component);
    //}
    //private void OnTriggerExit(Collider other)
    //{
       
    //    var other_unit_component = other.GetComponent<Unit>();
    //    var parent_unit_component = GetComponentInParent<Unit>();
    //    update_unit_target(parent_unit_component, other_unit_component);
    //}
    
    //private void update_unit_target(Unit potential_new_target, Unit parent_unit ) {

    //    if (potential_new_target == null || parent_unit == null)
    //    {
    //        if (parent_unit != null && parent_unit.targets_in_range.Contains(potential_new_target)){
    //           parent_unit.targets_in_range.Remove(potential_new_target);
    //        }
    //        return;
    //    }
    //    if (potential_new_target.faction != parent_unit.faction && potential_new_target != parent_unit)
    //    {
            
           
    //         parent_unit.find_closest_target_in_range();
            

    //    }
    //}
}
